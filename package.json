{
  "name": "support-agent",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "dev:api": "pnpm --filter @support-agent/api dev",
    "dev:worker": "pnpm --filter @support-agent/worker dev",
    "dev:gateway": "pnpm --filter @support-agent/gateway dev"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "gh": "^2.8.9"
  }
}

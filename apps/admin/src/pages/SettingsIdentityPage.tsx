import { PageShell } from '@/components/ui/PageShell'

export default function SettingsIdentityPage() {
  return (
    <PageShell title="Settings Identity">
      <div className="rounded-[var(--radius-lg)] border border-gray-100 bg-white p-10 text-center text-sm text-gray-400">
        Settings Identity — under construction
      </div>
    </PageShell>
  )
}
